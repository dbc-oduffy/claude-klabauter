---
name: Install problem
about: The engine would not install, or installed but will not run
title: "install: "
labels: install
---

## What failed

What you ran, and what it did instead of working.

## Command

```
paste the exact command
```

## Output

```
paste the full output, including the traceback if there was one
```

## Where it stopped

Which step — dependency resolution, the package build, the first invocation of
an entrypoint, something else?

## Environment

- OS and version:
- Python version (`python --version`):
- How Python was installed (system package, python.org, pyenv, conda, uv):
- Virtualenv in use? (yes/no, and which tool)
- Installed from: PyPI / a git clone / a downloaded archive
- Engine version:

## Already tried

Anything you ruled out — a clean virtualenv, a different Python, a fresh clone.

## Anything unusual about the machine

Corporate proxy, restricted network, no compiler toolchain, a non-standard home
directory, a network or synced filesystem, locked-down execution policy.
