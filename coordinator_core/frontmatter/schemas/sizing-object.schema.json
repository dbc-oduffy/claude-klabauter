{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coordinator.local/schemas/sizing-object.schema.json",
  "x-schema-name": "sizing-object",
  "x-schema-version": "1.12.0",
  "x-bump-class": "enum-value-additive",
  "x-bump-note": "1.11.0 -> 1.12.0 widens `detents.items.enum` with ONE value, `probe_raise_ask_scope_asserted`, APPENDED AT THE END after `probe_raise_on_substrate_condition` -- no re-sorting, enum ORDER is load-bearing against example-doctrine-repo's EQUAL_VERSION_SHAPE_DRIFT gate, which hashes list order verbatim. Classified `enum-value-additive`: one value appended to one existing closed enum, no field added, no field removed, no required-field change, no existing value's semantics changed -- every document valid under 1.11.0 still validates. WHAT IT CLOSES: the open counter recorded in the 1.10.0 -> 1.11.0 note below. `probe_raise_on_substrate_condition` costs the EM the raise, while `ask-scope` -- the notch-preserving answer -- cost nothing and was recorded nowhere queryable, an honesty gradient of exactly the kind that made `boundary_counted_in_notch` fail to bite. Both answers now leave a queryable mark and only one moves the size. ADVISORY, unlike its sibling: the raise it names has ALREADY applied, so its only job is to make the assertion findable. FIRING PREDICATE, narrower than the counter as sent and authoritative in both copies: it fires ONLY on an actual `raise`. An `ask-scope` basis passed with no raise, or alongside a `collapse`, describes a resize that never happened, and manufacturing a mark there would put noise in exactly the field a later reader queries for signal. CROSS-REPO ORDERING, and the stagger ran the other way this time: this vendored copy landed 1.12.0 first (claude-klabauter@73f98180264d, with the engine half), reddening example-doctrine-repo's `test_vendored_schema_version_parity` on version equality and `test_sizing_route_enum_schema_parity::test_detent_enum_parity` on set equality against `DETENT_ENUM`; example-doctrine-repo landed the matching bytes at example-doctrine-repo@f8c4b15b2 and both gates are green on both sides. THE MECHANISM THAT CLEARS IT: that helper reads the sibling's COMMITTED HEAD, never their working tree -- the clearing act is always a commit on the lagging side, and a bump sitting uncommitted in a dirty tree clears nothing while looking identical to not having done it. Spec backlink: cross-repo/inbox/2026-08-11-example-doctrine-repo-em-sizing-1-12-0-landed-stagger-closed.md. ---- PRIOR NOTE (1.10.0 -> 1.11.0 vendoring, this side): vendored example-doctrine-repo's 1.11.0 `detents.items.enum` widen (`intent_em_elaborated`, `precedent_shipped_before`, `probe_raise_on_substrate_condition`) order-exact at claude-klabauter@cf1d757c8ac6, clearing the parity red example-doctrine-repo was holding off `origin/main`. That same commit RETIRED the hold-off clause both chains had been copying forward, which claimed an unequal `x-schema-version` makes the gate go silent: it does not. `test_vendored_schema_matches_doe_source` asserts version equality HARD and only reaches the shape-hash compare once that passes, so BOTH stagger directions are red, not one red and one silent. Verified against their tree rather than taken on report. ---- PRIOR NOTE (1.9.0 -> 1.10.0 widens `status.enum` with ONE new terminal advisory value, `declined`, APPENDED AT THE END after `shipped` -- no re-sorting, enum ORDER is load-bearing against example-doctrine-repo's EQUAL_VERSION_SHAPE_DRIFT gate. `status.description` gains the paired prose distinguishing `declined` from `superseded` (sound-but-refused, no successor, nothing invalidated it) and from `shipped` (nothing landed) -- description is stripped by `_semantic_shape_hash` and is shape-invisible, so only the enum member itself is the shape change. Classified `enum-value-additive`: one value appended to one existing closed enum, no field added, no field removed, no required-field change, no existing value's semantics changed -- every document valid under 1.9.0 still validates. Motivating change: docs/plans/2026-08-10-a-terminal-status-for-a-declined-sizing.md § C2 -- a routed sizing whose spend was refused on direction-class grounds had no terminal status to land in, so `nudge_terminal_artifact_edit`'s own live-alternative offer (`_SIZING_TERMINAL_STATUSES`) could keep handing a declined sizing back as if it were still live; this bump gives that state a schema-level home. CROSS-REPO ORDERING, the same coupling every prior note on this file states: example-doctrine-repo's `test_vendored_schema_version_parity.py` compares shapes only at EQUAL `x-schema-version`; this vendored copy now reads 1.10.0 while example-doctrine-repo's copy is presumed to still read whatever it held before this change, so their parity gate compares nothing until they land their own half. Per this file's own established convention (see the 1.9.0 note's identical clause), the exact bytes here -- one enum value, `declined`, appended after `shipped`, plus the paired description prose -- are the agreed END STATE this bump commits to; do not bump `status.enum` again before example-doctrine-repo lands their matching side, or their gate goes silent against a moving target rather than green. This note does NOT touch `detents.items.enum` or any other property -- confirmed against the diff, which is `status`-only. A companion note on the same coupling: this bump was applied by review-integrator (not the original authoring session) after a code-reviewer finding (`state/subagent-share/a076d234-d507-4363-9a0c-84a35dee6836/coordinatorcode-reviewer-6490ec01.md`, finding P1b) caught that the `declined` enum widen had landed in an earlier commit without a paired version bump -- this note is that missed bump, applied after the fact, against the file as it stands rather than reconstructed from the original commit's intent. ---- PRIOR NOTE (1.8.0 -> 1.9.0) widens `detents.items.enum` with TWO new advisory values, `boundary_counted_in_notch` and `scout_evidence_mention_count`, APPENDED AT THE END in exactly that order after `goal_setting_pm_gated` — no re-sorting, since enum ORDER is load-bearing against example-doctrine-repo's EQUAL_VERSION_SHAPE_DRIFT gate. Nothing else in this file changes shape (the `detents` description gains prose for the two values; `description` is stripped by `_semantic_shape_hash` and is shape-invisible). Source: inbound memo cross-repo/inbox/2026-08-10-example-doctrine-repo-em-sizing-guard-flags.md (from example-doctrine-repo-em), asking claude-klabauter for two typed flags on `sizing-assemble` — `--boundary-in-notch yes|no` and `--scout-evidence-kind mention-count|change-set|site-count` — deliberately shaped as a replay of the `--premise-provenance` ask (2026-08-05 memo, landed). MOTIVATING INCIDENT, sender-reported: example-retrieval-repo-em sized an S ask as XL, justified by a published-artifact contract with a sibling, and resolved `route: pm-decision`; a one-sentence PM refutation re-resolved it to `S -> spec-dispatch` — two notches and three rooms of ceremony. The rule broken was already written in example-doctrine-repo's `coordinator/skills/sizing/SKILL.md` § *A cross-team dependency is a gate, not a size*, and they had READ that section in the same turn they violated it. That is the finding: prose guards are read at flow Step 1, before the EM has evidence to check them against, and nothing re-presents them at Step 3 when the notch is committed to a flag. A required answer that reaches a validator is what closes the gap. The second flag covers the same session's sharper error — a scout returned ~108 files that MENTION a literal and the count was passed as a change-set; under a back-compat shim every one of those resolves unchanged. Both new detents are ADVISORY ONLY, the unbroken house rule for this array: neither changes the resolved `route`, neither populates `xl_exit`, and neither fires on the `express_lane` path (which returns before any detent computation, by design D3). ASYMMETRY WORTH KNOWING, since it departs from the premise pair directly above: `boundary_counted_in_notch` is NOT size-gated, while `premise_unproven`/`premise_not_applicable` gate on a resized M-and-above. This is deliberate — the size is precisely the value a counted boundary is suspected of having inflated, so gating the check on that size exempts the reads the detent exists to catch (the motivating incident was an S sized XL; any M-and-above gate would exempt it the moment the collapse landed). NEGATIVE SPEC, carried from the memo verbatim because it is the thing most likely to be 'helpfully' undone later: `scout_evidence_kind` is a TYPED FIELD BESIDE the free text and is emphatically NOT licence to start parsing `scout_evidence` strings — claude-klabauter's standing constraint (`coordinator_core/sizing_assemble/__init__.py` negative-spec, 'Do NOT parse free-text scout_evidence strings for sizing signal') is exactly WHY the discriminator has to be a typed flag: the kind of a piece of evidence is not recoverable from its prose, by the engine or by anyone. The `resized_tshirt == 'XXL' and not scout_evidence` predicate still tests EMPTINESS only and stays that way. Classified `enum-value-additive`: two values appended to one existing enum, no field added, no field removed, no required-field change, no existing value's semantics changed — every document valid under 1.8.0 still validates. CROSS-REPO ORDERING, the live coupling this bump creates and the reason the exact bytes are pinned here: example-doctrine-repo's `coordinator/tests/test_sizing_route_enum_schema_parity.py::test_detent_enum_parity` asserts SET equality between their `coordinator/schemas/sizing-object.schema.json` `detents` enum and claude-klabauter's `coordinator_core.sizing_assemble.DETENT_ENUM`, so this engine-side widen turns THAT test red until example-doctrine-repo lands their half — which their memo states is theirs and not blocked on us. Their `test_vendored_schema_version_parity.py` compares shapes only at EQUAL `x-schema-version`, so while they sit at 1.8.0 and this copy reads 1.9.0 that gate compares nothing and the divergence is SILENT — the same transient the 1.6.0 -> 1.7.0 note below documents, and the same resolution: 1.9.0 is stamped here as the agreed END STATE, and example-doctrine-repo stamps 1.9.0 with these exact enum bytes to bring both gates green. Do not bump past 1.9.0 before they land, or their gate goes silent against a moving target rather than green. The reply memo carries these coordinates. ---- PRIOR NOTE (1.7.0 -> 1.8.0) vendors example-doctrine-repo's deliverable-spine addition (example-doctrine-repo@54e0576d8, coordinator/schemas/sizing-object.schema.json 1.7.0 -> 1.8.0) — the receive side of DR-097's push-side notification duty (their memo landed, DR-097 ping cross-repo/inbox/2026-08-10-example-doctrine-repo-em-c2-c9-landed-vendor-ping-and-our-ask-was-wrong.md). THREE additions, taken verbatim from example-doctrine-repo's bytes where new: (1) `deliverable_id`, the durable spine join key, first minted on this artifact per example-doctrine-repo's note; (2) `plan`, a nullable FK to the plan this sizing routed into, mirroring plan.schema.json's own `sizing_object` field by symmetry; (3) `shipped` appended to `status.enum` at the end (no re-sorting — enum order is instance-data under example-doctrine-repo's `_semantic_shape_hash`), with example-doctrine-repo's `status` description taken verbatim since it is what defines the new value. Per this schema's own standing rule, `x-bump-class` records the more restrictive of the bundled pair — the two new fields are `nested-field-additive`, the enum widen alone is `enum-value-additive` — so `nested-field-additive` is recorded. STRUCTURAL MERGE, NOT A BYTE COPY: this vendor deliberately does NOT pull example-doctrine-repo's independently-drifted prose elsewhere in this file (their `appetite`/`estimate.tshirt`/`route`/`detents`/`xl_exit`/`pm_resolution` descriptions have moved since the two sides' shared 1.7.0 — their `estimate.tshirt` no longer states the XXL=32 weight and their `xl_exit` no longer says four XL exits, both prose-only deltas invisible to `_semantic_shape_hash`, which strips `description`). Every document valid under 1.7.0 still validates; no required field added, removed, or narrowed. See docs/plans/2026-08-10-sizing-objects-join-the-deliverable-spine.md § C0. ---- PRIOR NOTE (1.6.0 -> 1.7.0) is a SHARED CROSS-REPO COORDINATE, not a local monotonic counter — example-doctrine-repo's `test_vendored_schema_version_parity.py` compares shapes only when the two sides' `x-schema-version` are EQUAL; unequal versions compare nothing and the divergence is SILENT (a failure mode this plan's C4 body names explicitly). Example-doctrine-repo has agreed to stamp 1.7.0 for the END STATE of this file after BOTH of the following changes land, and is holding their own bump off `origin/main` until this vendored copy carries exactly 1.7.0 — bumping past it to a local 1.8.0 would go silent against their gate rather than green. This single 1.7.0 bump therefore BUNDLES two changes landed minutes apart in the same session, not one: (a) the appetite half — `appetite` removed from the top-level `required` array, `post_size_prompt_pending` appended to `detents.items.enum` — landed first at commit `df8ccac36` (docs/plans/2026-08-07-appetite-leaves-the-front-of-sizing.md § C3), which briefly carried `x-schema-version: 1.7.0` on the strength of that change alone; (b) the XXL-notch half — this plan's C4 — supersedes that within the same session, widening THREE closed enums, all agreed with example-doctrine-repo (cross-repo/inbox/2026-08-07-example-doctrine-repo-em-xxl-notch-four-answers.md, item 3), APPENDED AT THE END, no re-sorting — enum order is load-bearing against example-doctrine-repo's EQUAL_VERSION_SHAPE_DRIFT gate: (1) `estimate.tshirt.enum` gains `XXL` (weight 32, continuing the doubling); (2) `route.enum` gains `goal-setting`, the sixth-notch's terminal room; (3) `detents.items.enum` gains `xxl_unprobed` and `goal_setting_pm_gated`, in that order, appended after `post_size_prompt_pending` (the appetite half's own addition, (a) above). Both halves are ADVISORY/additive — no field removed, no required-field added except `required`-array membership loss which retains the field itself, no existing value's semantics changed — so `enum-value-additive` is the correct class for the bundle, matching example-doctrine-repo's agreed class. This plan's own Anti-scope bans reopening the bilaterally-settled version number unilaterally — reversing it would break example-doctrine-repo's gate, the exact failure this note now avoids. Symmetric-parity coupling: `coordinator/tests/test_sizing_route_enum_schema_parity.py::test_detent_enum_parity` (DETENT_ENUM) and `coordinator/tests/test_vendored_schema_version_parity.py` (version pin) both gate on this file; example-doctrine-repo holds their own bump off `origin/main` until this vendored copy carries the agreed 1.7.0 shape. Spec backlink: docs/plans/2026-08-07-sizing-ladder-xxl-notch-and-goal-setting-route.md § C4. ---- PRIOR NOTE (1.6.0 -> 1.7.0, appetite half only, superseded within the same session by the bundled note above) bundles two changes landing together in the same commit as claude-klabauter's `coordinator_core.sizing_assemble.DETENT_ENUM` widen (see `docs/plans/2026-08-07-appetite-leaves-the-front-of-sizing.md` § Ordering constraint): (a) removed `appetite` from the top-level `required` array — a volunteered appetite is still valid and still validated via the unchanged `appetite` property/enum/description, but it is no longer a precondition for a sizing-object to validate, per example-doctrine-repo's PM ruling that appetite is not collected before a size is delivered (source memo: cross-repo/inbox/2026-08-07-example-doctrine-repo-em-appetite-leaves-the-front-of-sizing-engine-half.md); (b) widened the `detents[]` enum with one new advisory value, `post_size_prompt_pending`, APPENDED AT THE END of the existing seven-value list — enum ORDER is load-bearing against example-doctrine-repo's EQUAL_VERSION_SHAPE_DRIFT gate, which hashes list order verbatim, so this bump does not re-sort. `post_size_prompt_pending` is ADVISORY ONLY, same house rule as every other `detents` value: it never changes the resolved `route` and never populates `xl_exit`. CLASSIFICATION, AND THE GAP IT SURFACES: this schema's own established house rule (see the 1.1.0 -> 1.2.0 note below) is that a bundled bump records the MORE RESTRICTIVE of the classes in play — but that rule presupposes the house vocabulary (`../example-doctrine-repo/coordinator/docs/wiki/schema-version-gate.md` § bump-class table) names a class for every sub-change in the bundle, and confirmed at this bump: it does not. The vocabulary names exactly three classes — `nested-field-additive`, `enum-value-additive`, `major` — and `major` is defined there as 'a required field added, a field REMOVED, or existing semantics changed': a *field* removal, not a `required`-array membership removal that leaves the field, its enum, and its description all intact. Forcing `major` here would misdescribe what changed (no field was removed); forcing `enum-value-additive` alone without saying so would silently omit that the `required` array changed too. The house vocabulary, AS IT STANDS, does not name a class for 'required-field removal, field retained' — this note states that gap explicitly rather than inventing a label to fill it. Recorded `x-bump-class` is `enum-value-additive`: the sub-change that DOES have a correctly-fitting name, and the more conservative choice for a consumer since it already holds unconditionally on this axis with no declared-tolerance carve-out (schema-version-gate.md § holding table) — under-classing the bundle as anything weaker would be the wrong direction to err. SYMMETRIC-PARITY COUPLING, the sharpest live instance the vocabulary itself names: `../example-doctrine-repo/coordinator/docs/wiki/schema-version-gate.md:251-254` cites exactly this schema's `detents` enum mirrored against claude-klabauter's `coordinator_core.sizing_assemble.DETENT_ENUM`, guarded by `coordinator/tests/test_sizing_route_enum_schema_parity.py::test_detent_enum_parity` — either side widening alone turns that suite red. The version-pin side of the same coupling is `coordinator/tests/test_vendored_schema_version_parity.py`; `sizing-object.schema.json` is no longer in that test's `DOE_ONLY_SCHEMAS`, which is the second correction this note records. CORRECTING THE STALE PRIOR-NOTE RECORD: every `x-bump-note` entry below this one asserts 'claude-klabauter does not vendor this schema' (citing `DOE_ONLY_SCHEMAS` in `test_vendored_schema_version_parity.py`, re-verified at each of those bumps). That assertion is now STALE — this file, `coordinator_core/frontmatter/schemas/sizing-object.schema.json`, IS claude-klabauter's own vendored copy as of this bump, and DR-097's sibling-notification duty attaches to every future change here. The prior notes below are left unedited as an append-only historical record of what was true at each earlier bump; this paragraph states the current position going forward. Spec backlink: docs/plans/2026-08-07-appetite-leaves-the-front-of-sizing.md § C3. ---- PRIOR NOTE (1.5.0 -> 1.6.0) added TWO optional fields, both closing the same gap: a spike that discharges the `premise` object's `read` branch had no schema home for its output. `coordinator/skills/sizing/SKILL.md:68-73` prescribes routing a `read` premise to `coordinator:spike` before `coordinator:plan`, to convert the code-read premise into executed evidence ahead of planning — but the schema gave that ceremony's output nowhere to land, so an author improvised one ad hoc. This is the FOURTH occurrence of the identical failure mode this schema's own x-bump-note history documents: the 1.1.0 -> 1.2.0 note below records the first two (`surfaced_to_pm`, `pm_resolution`), and the 1.2.0 -> 1.3.0 note records the third (`superseded_note`). Motivating record: claude-klabauter/state/sizings/2026-08-06-uninstall-reverse-legs-12-14-16-17-22-24.yaml (read for shape only, not edited). Inbound memo: cross-repo/inbox/2026-08-06-claude-klabauter-em-sizing-object-schema-gaps-five-residue.md. Fields added: (1) `premise.spike_verdict`, a new optional nested string property inside the existing `premise` object, pointing to the spike-verdict record a `coordinator:spike` produced when it upgraded this sizing's premise from `read` to `executed`. NEGATIVE SPEC: it never substitutes for `evidence` — `evidence` stays required for every provenance value except `unrecorded`, and the inline citation remains the discharge per `premise`'s own house rule; `spike_verdict` is the structured pointer to the ceremony record, `evidence` is still what a reader reads. Absent when no spike ran. (2) `spike_amendments`, a new optional top-level array of objects (`scope`, `amendment`, both required strings), recording the BINDING SCOPE a `coordinator:spike` produced for the downstream plan — the routing intent the spike's executed evidence changed about how this work must be planned. `scope` names the sub-piece re-specced, or `cross-cutting`; always a string even for a numeric-looking identifier. NEGATIVE SPEC: not a substitute for the downstream plan's own scope or chunking, and not a place to record spike findings that changed nothing — an entry exists only because it re-specs something. Omit entirely when no spike ran; an empty array is not an amendment set. `system.created_by_session`/`sized_by_session`/`linked_sessions` (session provenance) was CONSIDERED AND DECLINED at this bump, not overlooked — reaffirming the 1.2.0 -> 1.3.0 note's identical ruling on `system.created_by_session`, which is recoverable from the commit's own `Session-Id:` trailer and needs no schema slot. Classified `nested-field-additive` per this schema's own vocabulary — one new optional top-level array (`spike_amendments`) plus one new optional nested string (`premise.spike_verdict`); every document valid under 1.5.0 still validates. Claude-klabauter does not vendor this schema (DOE_ONLY_SCHEMAS in coordinator/tests/test_vendored_schema_version_parity.py, re-verified at this bump), so DR-097's sibling-notification duty does not attach. Greppability: coordinator/skills/sizing/SKILL.md now names both fields as the landing surface for a spike's discharge, per CLAUDE.md § Conventions. Spec backlink: cross-repo/inbox/2026-08-06-claude-klabauter-em-sizing-object-schema-gaps-five-residue.md. ---- PRIOR NOTE (1.3.0 -> 1.5.0, squashed, single commit) BUNDLES two sub-changes that landed together rather than as staggered pins: (a) a REQUIRED top-level `premise` object (`major`-class — see below) and (b) an `enum-value-additive` widen of the `detents[]` enum with TWO new advisory values, `premise_unproven` and `premise_not_applicable`. Per this schema's own established rule from its 1.1.0 -> 1.2.0 note (\"the recorded `x-bump-class` above takes the more restrictive of the two\"), `x-bump-class` records `major` — the strictly more-restrictive of the bundled pair, holding unconditionally on every axis per docs/wiki/schema-version-gate.md, versus `enum-value-additive`'s weaker holding duty. Do not read the widen alone as having set the class: `x-bump-class` was originally recorded as `nested-field-additive` by the commit that performed this bump (dbccb35e8), then relabeled `enum-value-additive` by a follow-up commit (48c074749) that used the version-pin regenerator's `--force --reason` path to accept the resulting shape-hash move under an unmoved `x-schema-version` — neither prior value was ever `major`, despite this same note's premise-field paragraph below classifying that sub-change as `major` from the start. This entry corrects that post-hoc misclassification; `x-schema-version` stays 1.5.0 unmoved, since no new shape change is being introduced here, only the recorded class being fixed to match what actually shipped. `detents[]` widen: unblocked because claude-klabauter's `coordinator_core.sizing_assemble.DETENT_ENUM` carries both values (claude-klabauter/coordinator_core/sizing_assemble/__init__.py:160) — two values added to the existing `detents[]` enum, no field added, no field removed, no existing value's semantics changed. Deliberately does NOT fold into C1's 1.3.0 -> 1.4.0 bump: that bump landed before claude-klabauter's DETENT_ENUM widen was verified, so its own note explicitly deferred `detents` to this separate bump; giving the enum widen its own version increment keeps the parity test's pass/fail boundary aligned to the commit that actually changed `detents`, rather than backdating the change onto a bump that predates it. Both values are ADVISORY ONLY, matching the `premise` object's own house rule (DR-068's advisory-not-blocking precedent) — they mirror `premise.provenance`'s `unproven`/`not-applicable` outcomes into a queryable detent but never change the resolved `route` and never populate `xl_exit`. `coordinator/tests/test_sizing_route_enum_schema_parity.py::test_detent_enum_parity` is the coupling test this bump satisfies — it asserts SET equality (order cosmetic) between this enum and `DETENT_ENUM`; both sides now carry both values. Claude-klabauter does not vendor this schema (DOE_ONLY_SCHEMAS in test_vendored_schema_version_parity.py, re-verified at this bump), so DR-097's sibling-notification duty does not attach. Spec backlink: docs/plans/2026-08-05-premise-provenance-gate-above-m.md § C1b. ---- PRIOR NOTE (1.3.0 -> 1.4.0, folded into the same squashed commit above) added a REQUIRED top-level `premise` object (`provenance`: closed enum executed/read/not-applicable/unrecorded, plus a conditionally-required `evidence` string) recording whether this sizing's premise was ever actually verified before structure was built on top of it. Classified `major` per this schema's own x-bump-note vocabulary: adding a REQUIRED field means a document valid under 1.3.0 (lacking `premise` entirely) no longer validates — the same test a required-field add always fails, as the 1.0.0 -> 1.1.0 note below already established for `xl_exit`. Motivating incident: claude-klabauter state/lessons/2026-08-05-a-plan-larger-than-m-built-on-an-unreproduced-root-cause-is-a-spike-wearing-a-plan-s-clothes.yaml — a plan larger than M was built on a root cause nobody had reproduced, and nothing at sizing time asked where the premise underneath the plan actually came from. Deliberately does NOT touch the `detents` enum in this same change — a sibling chunk (C1b) owns widening `detents` with `premise_unproven`, held on claude-klabauter's `DETENT_ENUM` widen landing first. THIS repo's own coupling test, `coordinator/tests/test_sizing_route_enum_schema_parity.py::test_detent_enum_parity`, asserts this schema's `detents` enum stays byte-identical to claude-klabauter's `coordinator_core.sizing_assemble.DETENT_ENUM` — that parity test staying green through this bump is the proof `detents` was left alone. Practical blast radius: every existing state/sizings/*.yaml record is migrated in the same window to carry `premise: {provenance: unrecorded}` (the one provenance value that needs no `evidence`, since it exists purely to mark records that predate this field). Claude-klabauter does not vendor this schema (DOE_ONLY_SCHEMAS in test_vendored_schema_version_parity.py, re-verified at this bump), so DR-097's sibling-notification duty does not attach. Spec backlink: docs/plans/2026-08-05-premise-provenance-gate-above-m.md § C1. ---- PRIOR NOTE (1.2.0 -> 1.3.0) added ONE optional top-level property, `superseded_note` (string), recording WHY a sizing-object was superseded. The schema's `status` enum already names `superseded` as a lifecycle state but, before this bump, gave no field to record the reason — so authors improvised one, ad hoc, each time. This is the THIRD occurrence of the identical failure mode this schema's own history documents (see the 1.1.0 -> 1.2.0 note below for the first two, `surfaced_to_pm` and `pm_resolution`): state/sizings/2026-08-03-dr-100-auto-reconcile-dry-run-flip-disch.yaml was committed carrying an ad-hoc top-level `superseded_note` (plus an ad-hoc `system.created_by_session`, dropped rather than schematized — recoverable from the commit's own `Session-Id:` trailer, so it needed no schema slot) and was failing `additionalProperties: false` from the moment it landed. Deliberately NOT folded into `pm_resolution`: a supersession reason is EM-observed process history about the sizing-object itself, not a PM-owned decision reasoning record, and folding it in would misfile it under the wrong provenance. Classified `nested-field-additive` per the wiki's own discriminator (a single new optional top-level string field) — every document valid under 1.2.0 still validates. Claude-klabauter does not vendor this schema (DOE_ONLY_SCHEMAS in test_vendored_schema_version_parity.py, re-verified at this bump), so DR-097's sibling-notification duty does not attach. ---- PRIOR NOTE (1.1.0 -> 1.2.0) added TWO optional top-level properties, both recording PM-owned material the schema previously had nowhere to put: `surfaced_to_pm` (an array of direction-class items surfaced but NOT decided) and `pm_resolution` (an object holding the PM's reasoning once they HAVE decided). This bump touches two classes: `surfaced_to_pm` is `top-level-array-additive` on its own, `pm_resolution` (a new top-level object) is `nested-field-additive` per the wiki's own discriminator (type of field added, not optionality). The recorded `x-bump-class` above takes the more restrictive of the two, `nested-field-additive` — holding unconditionally on the `x-schema-version` axis per docs/wiki/schema-version-gate.md § Additive-bump holding/non-holding split, with no declared-tolerance carve-out for that class on this axis. Both fields are new and optional, so every document valid under 1.1.0 still validates — that is the backward-compat argument, distinct from (and not sufficient to determine) the class-vocabulary/holding-obligation argument above. Motivating incident (2026-08-03): a sizing carried a live direction-class item — whether a real provenance-smearing incident should reopen the 2026-07-31 PM ruling that COORDINATOR_SCOPE_STRICT stays off — and `additionalProperties: false` with no home for it forced the reasoning into a YAML comment, invisible to any query over state/sizings/. A surfaced item that cannot be queried is a forgotten item. `pm_resolution` closes the same gap from the opposite side and was NOT speculative: state/sizings/2026-08-02-hook-operator-message-size-cap.yaml (committed 8f32cbffa) already carried an ad-hoc `pm_resolution` block and had been failing test_sizing_objects_validate_against_schema against 1.1.0 ever since. Two independent sessions inventing structure for PM decisions is what promoted this from a nicety to a defect. Deliberately NOT folded into `detents` (a closed enum of lobby-computed boundary stops) nor into `fork`/`xl_exit` (the two halts the engine itself resolves), because this is EM-observed and PM-owned rather than engine-computed. Claude-klabauter does not vendor this schema (DOE_ONLY_SCHEMAS in test_vendored_schema_version_parity.py, re-verified at this bump), so DR-097's sibling-notification duty does not attach. ---- PRIOR NOTE (1.0.0 -> 1.1.0, bump-class `major`): added `xl_exit` as a REQUIRED property (the PM's recorded choice among the four XL exits when `route` is `pm-decision`), and widened two closed enums: `route` gained `spec-dispatch` and `pm-decision`, and `detents[]` gained `pm_decision_pending`. Classified `major` on the vocabulary's own terms — a required field was added, so a document valid under 1.0.0 that omits `xl_exit` no longer validates. The enum widens alone would have been additive; the required-field add is what carries the class. Practical blast radius zero: all 10 records under state/sizings/ were migrated to carry `xl_exit` in the same change. Recorded at a MINOR version increment rather than 2.0.0, matching this repo's existing practice for a major-class change with a migrated corpus (priority-intent.schema.json 1.1.0, priority-ledger.schema.json 1.1.0, roadmap.schema.json 1.2.0 all carry `x-bump-class: major` at a minor increment). Claude-klabauter does not vendor this schema (it is in test_vendored_schema_version_parity.py's DOE_ONLY_SCHEMAS), so DR-097's sibling-notification duty does not attach to this bump. Spec backlink: docs/plans/2026-07-24-sizing-lobby-core.md.",
  "applies_to": "state/sizings/*.yaml",
  "description": "Sizing-object artifact — the first-class routing artifact for the fleet routing lobby. Captures the PM's verbatim intent, the Shape-Up-style appetite BUDGET, a coarse provisional t-shirt routing estimate, and which room (dispatch/shape/plan/roadmap) the work should enter. Per-repo session-produced routing state, same taxonomy class as state/handoffs/ — see docs/wiki/state-placement-law.md § Taxonomy. Spec backlink: docs/plans/2026-07-24-sizing-lobby-core.md § C1 (Design D1/D2/D3).",
  "type": "object",
  "required": [
    "schema",
    "intent",
    "estimate",
    "route",
    "detents",
    "fork",
    "xl_exit",
    "status",
    "premise"
  ],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "type": "string",
      "const": "sizing-object",
      "description": "Schema discriminator — always 'sizing-object'. Enables kind-first schema matching."
    },
    "intent": {
      "type": "string",
      "description": "The PM ask, verbatim. Not paraphrased or summarized — the raw framing the sizing lobby routed on."
    },
    "appetite": {
      "type": "string",
      "enum": [
        "small",
        "medium",
        "large"
      ],
      "description": "The Shape-Up BUDGET the PM MAY state for this work — never collected before the size is delivered, and never a precondition for this sizing-object to validate (see `required`, which does not name this field). Its absence is a legitimate state meaning 'not yet asked, or not volunteered'; it is recorded once the PM has answered the open post-size prompt. When the PM does state one, it is a FIXED BUDGET, NOT a reason to defer scope and NOT 'appetite-based OOS' (which coordinator doctrine bans — see coordinator/docs/wiki/implementation-standards-by-domain.md § Cross-cutting standards: 'OOS framing must be architectural, not appetite-based'). Appetite bounds how much time/effort is worth spending; it never justifies cutting a correctness/integrity requirement out of scope. INDEPENDENT OF estimate.tshirt: appetite is the PM's budget, the estimate is the EM's read of engineering complexity, and neither is an input to the other. On the default path that independence is now STRUCTURAL — there is no appetite in hand when the estimate is set — and it still holds on the volunteered path: a large appetite is not evidence the work is large; a small one never shrinks the read."
    },
    "estimate": {
      "type": "object",
      "required": [
        "tshirt",
        "provisional"
      ],
      "additionalProperties": false,
      "description": "Coarse routing-only size estimate. Distinct from the committed plan-body LoE (two-altitude model, D1.3): this estimate exists to pick which room (route) the work enters, not to bind the eventual plan's effort accounting.",
      "properties": {
        "tshirt": {
          "type": "string",
          "enum": [
            "XS",
            "S",
            "M",
            "L",
            "XL",
            "XXL"
          ],
          "description": "T-shirt size estimate. Reuses the existing loe.tshirt enum and weights verbatim (XS=1, S=2, M=4, L=8, XL=16) — see coordinator/schemas/completion-entry.schema.json loe.tshirt. Do not invent a parallel scale. `XXL` is the sixth notch and is ROUTING-ALTITUDE ONLY: it names an ask whose PLAN ITSELF exceeds one window, and it resolves directly to the `goal-setting` room rather than to any XL exit. It is ALSO a real notch on the effort ladder (coordinator/config/loe-thresholds.yaml), not vocabulary borrowed one-way into `loe.tshirt`: `chain_loe.tshirt` carries it too, because a chain's summed dispatches/tokens reach it routinely even though one session rarely does. An earlier revision of this sentence claimed XXL was never valid as a completed-work level of effort and that `chain_loe.tshirt` stayed XS–XL for that reason; that was retracted at completion-entry 1.4.0 — it described a ladder that saturated at XL rather than one that had been deliberately capped. Set from engineering complexity ALONE — surfaces touched, proven-vs-novel pattern, number of independent pieces. On the default path there is no appetite to anchor to, because none is collected before the size is delivered; where the PM has volunteered one unprompted, it is still not an input, and a t-shirt landing on the same notch as that volunteered budget is the anchoring tell, not a size."
        },
        "provisional": {
          "type": "boolean",
          "const": true,
          "default": true,
          "description": "Always true. Marks this tshirt value as a COARSE, revisable ROUTING estimate (which room to enter) — never the committed plan-body LoE. A plan authored downstream re-estimates independently; this field is not inherited as authoritative effort."
        }
      }
    },
    "route": {
      "type": "string",
      "enum": [
        "dispatch",
        "spec-dispatch",
        "shape",
        "plan",
        "roadmap",
        "pm-decision",
        "goal-setting"
      ],
      "description": "The room this sizing routes the work into: dispatch=small enough to execute directly, no artifact; spec-dispatch=small enough for a light plan artifact then dispatch, no Opus plan review; shape=problem not yet converged, needs /shape first; plan=decision-weight work needing coordinator:plan; roadmap=initiative-scale, needs roadmap-planning; pm-decision=initiative-scale and not a room by itself — the engine picks no room and the PM chooses among the offered exits (see `xl_exit`); goal-setting=the XXL room, needing coordinator:goal-setting to produce ratified OKRs plus roadmap-seed stubs. `goal-setting` is a resolved room, not a fifth XL exit — XL has four exits and is therefore a PM decision, XXL has exactly one room and is therefore a determinate map. NEGATIVE SPEC, cross-repo load-bearing: `goal-setting` is deliberately EXCLUDED from claude-klabauter's `nudge_unrouted_sizing._ROUTABLE_ROUTES` (routes whose room an EM may enter without a PM utterance), because `coordinator/skills/goal-setting/SKILL.md` is PM-GATED. Un-gating that skill here reds coordinator/tests/test_goal_setting_pm_gate_is_cross_repo_load_bearing.py, which routes the editor to memo claude-klabauter and hold the merge — never to relax the assertion."
    },
    "detents": {
      "type": "array",
      "description": "Boundary detents crossed or acknowledged while sizing this work — the discrete stops the sizing lobby snapped to (e.g. appetite/estimate divergence, route-boundary crossing) before landing on the final route. Empty array when no boundary was crossed. `premise_unproven` and `premise_not_applicable` are ADVISORY: they mirror the `premise.provenance` value (see the `premise` object) into a queryable detent, but never change the resolved `route` and never populate `xl_exit`. `post_size_prompt_pending` is the POSITIVE, engine-emitted signal that the size has been delivered at M/L/XL with no appetite recorded and the EM is waiting on the PM's open post-size answer — it is what every downstream consumer keys the open-halt exemption on. NEVER infer that halt from `appetite`'s absence instead: absence is the shape every M+ record carries by construction, whether or not a size was ever delivered or a question ever asked. `xxl_unprobed` and `goal_setting_pm_gated` are likewise ADVISORY, never blocking. `xxl_unprobed` fires when an XXL resolved with NO recorded `scout_evidence` — it watches the absence of EVIDENCE, not the absence of a PROBE, because the engine observes only the probe string and never whether one was justified; that predicate covers both reachable cells, the XL→XXL probe-raised promotion and the no-op raise clamped at the ladder top. `goal_setting_pm_gated` records that this sizing landed in a room the EM cannot enter without a PM utterance, closing a monotonicity inversion in which an XL carried a queryable 'a PM must speak' marker (`pm_decision_pending`) while a strictly larger XXL carried none. `boundary_counted_in_notch` and `scout_evidence_mention_count` are ADVISORY on the same terms: the first fires when a cross-repo boundary/memo/relay/assent gate contributed to the notch (a memo is a GATE, not a size — only negotiated co-design earns the notch), the second when the scout evidence behind the size is a mention-count rather than a change-set. Unlike the premise pair, `boundary_counted_in_notch` is NOT size-gated — the size is the thing a counted boundary is suspected of having inflated, so gating on it would exempt the reads the detent exists to catch. `intent_em_elaborated` and `precedent_shipped_before` are ADVISORY on those same terms: the first fires when the recorded `intent` is the EM's restatement rather than the PM's own words (`--intent-source em-elaborated`), the second when the ask repeats an operation whose runbook exists and has already landed (`--precedent shipped-before`) — a repeat is dispatch-shaped by default, but never auto-collapsed, because a repeat can genuinely be larger and the engine cannot see which. Neither is size-gated, for `boundary_counted_in_notch`'s reason: the size is what an elaborated intent is suspected of having inflated. `probe_raise_on_substrate_condition` is the ONE value in this array that is not advisory — it records that a Step 2 probe `raise` declared `--probe-raise-basis substrate-condition` was NOT applied, leaving the notch where the gut-read put it. That suppression is a READ discriminator, not an unread one: the caller applied the discriminator by choosing the value, and an area's health is not a size signal by definition. The route still resolves from the t-shirt→route table, so the Hard Gate holds — the SIZE moved, the route did not get overridden. An EM whose ask genuinely grew answers `ask-scope`, keeps the raise, and has made a recorded falsifiable assertion — recorded as `probe_raise_ask_scope_asserted`, which is ADVISORY and fires when a probe `raise` declared `--probe-raise-basis ask-scope` was APPLIED. It changes nothing: the raise has already moved the notch, and its only job is to make the assertion findable, so that both answers to the basis question leave a queryable mark and only one costs the raise. It fires ONLY on an actual `raise` — an `ask-scope` basis passed with no raise, or alongside a `collapse`, describes a resize that never happened, and marking it would put noise in exactly the field a later reader queries for signal.",
      "items": {
        "type": "string",
        "enum": [
          "appetite_conform",
          "appetite_exceeded",
          "route_boundary_crossed",
          "scope_boundary_acknowledged",
          "pm_decision_pending",
          "premise_unproven",
          "premise_not_applicable",
          "post_size_prompt_pending",
          "xxl_unprobed",
          "goal_setting_pm_gated",
          "boundary_counted_in_notch",
          "scout_evidence_mention_count",
          "intent_em_elaborated",
          "precedent_shipped_before",
          "probe_raise_on_substrate_condition",
          "probe_raise_ask_scope_asserted"
        ]
      }
    },
    "fork": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "string",
          "enum": [
            "cut_to_fit",
            "raise_appetite"
          ]
        }
      ],
      "description": "Null unless appetite and estimate diverge (estimate's implied size exceeds or undershoots the appetite budget). When set, this is UNRESOLVED BY CONSTRUCTION — the sizing skill surfaces the fork to the PM, it never auto-resolves it. cut_to_fit=trim scope to fit the existing appetite; raise_appetite=the PM increases the budget instead."
    },
    "xl_exit": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "string",
          "enum": [
            "split",
            "shape",
            "roadmap",
            "accept_multi_session"
          ]
        }
      ],
      "description": "The PM's recorded choice among the XL exits when `route` is `pm-decision`. The engine NEVER sets this — it always emits `null`, exactly as it does for `fork`. `null` is a legitimate open state meaning the PM has not chosen yet; it NEVER means the multi-session exit was accepted by default. Three exits are CURRENTLY OFFERED, each with a stated precondition: shape=the job-to-be-done is not stated, or the EM cannot falsifiably restate the problem in the PM's vocabulary; roadmap=the work spans >=2 named workstreams, or carries (or needs) an initiative/goal FK — a programme of plans, not one plan; accept_multi_session=none of the other preconditions hold and the PM has explicitly assented — writing this value IS that record. The fourth value, split, is RETAINED AS LEGACY VOCABULARY ONLY: it is not an offered exit and must not be re-offered (see this schema's `x-bump-note` for when and why it was withdrawn) — it remains a valid enum value solely so existing records that already carry it continue to validate (precondition history: docs/wiki/sizing-lobby.md § pm-decision). An ask that decomposes into independently shippable pieces routes to roadmap."
    },
    "scout_evidence": {
      "type": "array",
      "description": "Pointers to cartography/scout output that informed this sizing (file paths, wiki citations, prior-art references). Empty array when no scout ran.",
      "items": {
        "type": "string"
      }
    },
    "surfaced_to_pm": {
      "type": "array",
      "description": "Direction-class items the sizing surfaced to the PM but deliberately did NOT decide. Distinct from `fork` and `xl_exit`, which are the two structured halts the lobby computes for itself: this array is for everything else the EM noticed while sizing that is the PM's call — a standing ruling the new evidence bears on, a product-direction question the work would force, an external-facing consequence. NEGATIVE SPEC: this is never a record of a decision. An entry stays listed until it is resolved in its own artifact (a decision record, a PM ruling, a downstream plan); resolving it by editing the sizing-object is the failure this field exists to prevent. Once resolved, the PM's reasoning moves to `pm_resolution`. Empty array when nothing was surfaced. Exists because the alternative — a YAML comment — is invisible to any query over state/sizings/, which is how a surfaced item silently becomes a forgotten one.",
      "items": {
        "type": "object",
        "required": [
          "item",
          "why_not_decided"
        ],
        "additionalProperties": false,
        "properties": {
          "item": {
            "type": "string",
            "description": "The direction-class question itself, stated so a reader who was not in the session can act on it. Not a topic label."
          },
          "why_not_decided": {
            "type": "string",
            "description": "Why the EM did not settle this while sizing — e.g. a standing PM ruling this evidence bears on but does not overturn, product direction, an irreversible/external action, or a genuine no-correct-answer tradeoff. 'Not now' is not a reason; if the only reason is cost, it is break-class work to be fixed, not a direction-class item to be surfaced."
          },
          "prior_ruling": {
            "type": "string",
            "description": "Optional pointer to the existing decision, ruling, or audit entry this item bears on (file:line, DR-id, or artifact path). Present when the item revisits settled ground rather than opening new ground — its absence means no prior ruling was found, not that none exists."
          }
        }
      }
    },
    "pm_resolution": {
      "type": "object",
      "required": [
        "decided_on"
      ],
      "description": "The PM's recorded answer once they have resolved this sizing's open questions — the counterpart to `surfaced_to_pm`, which holds what is still open. Free-form string values by design: what a PM settles at a sizing varies (which fork, which XL exit, where an artifact should live), and a closed property list would push the next unanticipated decision back into an ad-hoc key or a comment. NEGATIVE SPEC: `fork` and `xl_exit` remain the AUTHORITATIVE machine-readable record of those two choices — an entry here carries the PM's REASONING, never a substitute value, and must not contradict them. Omit entirely when nothing has been resolved; an empty object is not a resolution.",
      "properties": {
        "decided_on": {
          "type": "string",
          "description": "Date the PM actually decided (YYYY-MM-DD), not the date the sizing was authored."
        }
      },
      "additionalProperties": {
        "type": "string",
        "description": "One resolved question, keyed by what it resolved (e.g. `post_size_prompt`, `xl_exit`, `wiki_home`). Value states the choice AND the reasoning, so a later reader need not reconstruct why."
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "sized",
        "routed",
        "superseded",
        "shipped",
        "declined"
      ],
      "description": "Sizing-object lifecycle. draft=intent captured, not yet estimated; sized=estimate+appetite set, fork may be outstanding; routed=route chosen and handed off (fork, if any, resolved); superseded=replaced by a later sizing for the same intent; shipped=terminal — the downstream work this sizing routed into has shipped; declined=terminal — routed, then the spend was refused on direction-class grounds. declined is NOT superseded: the sizing was sound and has no successor, nothing invalidated it, `superseded_note`'s 'verification that invalidated it' does not apply. declined is NOT shipped: nothing landed. Always paired with a decision-record backlink recording why."
    },
    "deliverable_id": {
      "anyOf": [
        {
          "type": "string",
          "pattern": "^dlv-(?!placeholder-replace-with)[0-9a-zA-Z][0-9a-zA-Z.-]*$"
        },
        {
          "type": "null"
        }
      ],
      "description": "Durable deliverable join key. Minted once at the earliest artifact and carried verbatim by every downstream artifact — a sizing-object is now the earliest artifact in the deliverable chain, so this is where the key is first minted. Null for pre-backfill records. Spec backlink: docs/plans/2026-08-08-sizing-objects-join-the-deliverable-spine.md § C2."
    },
    "plan": {
      "anyOf": [
        {
          "type": "string",
          "pattern": "^docs/plans/.+\\.md$"
        },
        {
          "type": "null"
        }
      ],
      "description": "FK to the plan (docs/plans/<name>.md) this sizing-object routed into. Optional and nullable. NEGATIVE SPEC: this is never a substitute for `deliverable_id` — `plan` is the human-readable pointer to one downstream artifact, `deliverable_id` is the spine key that actually joins the chain. Mirrors plan.schema.json's own `sizing_object` field by symmetry (that field points a plan back at its originating sizing-object; this one points forward at the plan it routed into). Spec backlink: docs/plans/2026-08-08-sizing-objects-join-the-deliverable-spine.md § C2."
    },
    "superseded_note": {
      "type": "string",
      "description": "Present only when `status` is `superseded`. Records WHY this sizing-object was superseded — the verification that invalidated it, the root cause, and where the live surface moved to. Distinct from `pm_resolution`: this is EM-observed process history about the sizing-object's own lifecycle, not the PM's decision reasoning, and must not be folded into `pm_resolution`."
    },
    "premise": {
      "type": "object",
      "description": "Whether the premise this sizing is built on was ever actually verified, before structure gets built on top of a guess. ADVISORY ONLY — the detent this can produce downstream (`sizing_assemble`'s `premise_unproven`, engine-side) NEVER blocks a route; it never changes the resolved `route` and never populates `xl_exit`. Discharge is by CITING THE EXECUTED EVIDENCE INLINE in the `evidence` field below — a file:line, test name, command output, or read location the author actually looked at — NEVER by producing a separate spike-result record; a premise question is answered in place, not spun into its own artifact. `provenance`: executed=the premise was verified by actually running/reproducing it; read=the premise rests on reading code/docs/a prior record, not on execution; not-applicable=this ask has no premise to verify (state the reason in `evidence`); unrecorded=migration-only, meaning this record predates this field and its premise provenance was never captured. DR-068 (docs/decisions/DR-068-commit-time-coverage-gate-scoped-shift-left-ambient-handoff-flat-fallback-advisory.md) is the standing precedent for this advisory-not-blocking shape — that ruling made the same call for the commit-time DAG verdict, and this field follows the same house answer rather than inventing a new one.",
      "additionalProperties": false,
      "required": [
        "provenance"
      ],
      "properties": {
        "provenance": {
          "type": "string",
          "enum": [
            "executed",
            "read",
            "not-applicable",
            "unrecorded"
          ],
          "description": "executed=verified by actually running/reproducing it; read=rests on reading code/docs/a prior record, not execution; not-applicable=this ask has no premise to verify; unrecorded=migration-only, the record predates this field."
        },
        "evidence": {
          "type": "string",
          "minLength": 1,
          "description": "The executed evidence cited inline (file:line, test name, command output, or read location) — or, when `provenance` is `not-applicable`, why. Required for every `provenance` value except `unrecorded`."
        },
        "spike_verdict": {
          "type": "string",
          "description": "Path to the spike-verdict record produced by `coordinator:spike` when a spike upgraded this sizing's premise from `read` to `executed`. NEGATIVE SPEC: never substitutes for `evidence` — `evidence` stays required for every provenance value except `unrecorded`, and the inline citation remains the discharge (see this object's own \"answered in place, not spun into its own artifact\" rule). `spike_verdict` is the structured pointer to the ceremony record; `evidence` is still what a reader reads. Absent when no spike ran."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "provenance": {
                "not": {
                  "const": "unrecorded"
                }
              }
            }
          },
          "then": {
            "required": [
              "evidence"
            ]
          }
        }
      ]
    },
    "spike_amendments": {
      "type": "array",
      "description": "BINDING SCOPE for the downstream plan — the discharged routing intent a `coordinator:spike` produced, recorded where the plan-author will read it. NEGATIVE SPEC: not a substitute for the downstream plan's own scope or chunking, and not a place to record spike findings that changed nothing — an entry exists because it re-specs something. Omit entirely when no spike ran; an empty array is not an amendment set.",
      "items": {
        "type": "object",
        "required": [
          "scope",
          "amendment"
        ],
        "additionalProperties": false,
        "properties": {
          "scope": {
            "type": "string",
            "description": "What this amendment binds: the named sub-piece of the ask it re-specs, or `cross-cutting` for one that binds the whole. Always a string, even when the underlying identifier is numeric."
          },
          "amendment": {
            "type": "string",
            "description": "The binding routing intent the spike produced for this scope: what the spike's executed evidence changed about how this work must be planned."
          }
        }
      }
    }
  }
}
