# DR-276 baseline — operator-CLI entrypoints that import coordinator_core.ops
# in-process WITHOUT routing through coordinator_core.cli_entry.run_op_main.
#
# These began as the trampolines the 2026-08-06 triage classified as bucket (c)
# (`state/audits/operator-cli-triage/`): a shared, near-byte-identical template,
# not independent decisions — a KNOWN backlog. Converting one means deleting its
# line here.
#
# 2026-08-06, end of the conversion sweep: 110 of the original 155 were
# converted (see git log for DR-276). **The 45 (now 44 -- `detect-staged-
# rollback.py` deleted 2026-08-25, "the staged rollback gate dies without
# blocking a commit") that remain are no longer an
# undifferentiated backlog** — every one was individually classified during that
# sweep, with per-file evidence in `state/subagent-share/`. They fall in four
# kinds, and the practical consequence is that this list is now mostly
# PERMANENT, not pending:
#
#   1. Structurally read-only — the op has no write site to declare, so routing
#      buys an import hop and a permanently-empty declaration list.
#   2. Already on the wire path — reaches the engine via `cc_invoke.route` /
#      `route_mutation` / `dispatch_message`, the very chokepoint this seam
#      backfills for in-process importers. Converting would fork a second
#      recording path, which is the harm, not the fix.
#   3. Mutates git refs or the index, never file content (branch renames,
#      `git config`, worktree reaping) — nothing a scope-touch claim models.
#   4. Genuinely deferred, and only these: `publish.py` (owns a 4716-line
#      `main()`; its writes land in sibling repos across dozens-to-hundreds of
#      files per target, so whether cross-repo publish output belongs in the
#      calling session's claim is a design question, not a mechanical one) and
#      `archive-stamp-cli` (every subcommand's write lands 2+ layers down in
#      ~15 handler-owned op modules; a partial pass would silently miss some).
#
# Do not re-attempt kinds 1-3 as if they were backlog. Kind 4 is the only real
# residual.
#
# The gate in test_operator_cli_engine_contact.py fails only on entries NOT in
# this file, so regrowth is caught while conversion stays incremental.
#
# Adding a line to this file is a carve-out. Say why, inline, or it will be
# deleted by the next reader as unexplained.
#
# SANCTIONED CARVE-OUTS — the entries below are NOT backlog. Each imports
# coordinator_core.ops in-process for a reason the seam does not address, and
# each is classified bucket (a), correct as-is. They are permanent; do not
# "convert" them.
#
# `update-docs-probes.py` (triaged 2026-08-06, was UNTRIAGED): imports gate
# FUNCTIONS from coordinator_core/ops/updatedocs_gates.py, which is a library
# of `_gate_*` predicates with no `main(argv)` for run_op_main to route to. The
# CLI's actual job is per-subcommand exit-code translation — the same
# GateVerdict maps to 0 in one subcommand and 1 in another — which is CLI-
# boundary logic the op deliberately leaves to its callers. It writes nothing
# itself; its one mutating path shells out to prune-resolved-queue-entries.py,
# which declares the write at the real write site.
#
# `cartography.py`'s 2026-08-06 triage note — "its only coordinator_core.ops
# import is `_registry_map.OP_MODULE_MAP`, a static data map read at call time,
# not an op invocation" — turned out to describe a whole CLASS, not one file.
# The gate's `_imports_an_op_in_process` credited ANY import from the package,
# so a private helper, a constant enum, or a path-derivation function read as
# an op reach. That predicate now requires an op ENTRYPOINT (see the function's
# own docstring for the four live specimens and the fail-closed narrowness),
# and these four left this file as a result — they were never bypasses:
#   cartography.py (`_registry_map.OP_MODULE_MAP`),
#   archive-stamp-cli (`handoff_stamp._SHIPPED_IN_KIND_ENUM`),
#   handoff-archive-transition.py (`fleet._common.handoff_archive_dest`),
#   query-records.py (`records_query._TYPE_TO_GLOB`).
# Carry-forward: `archive-stamp-cli` is still kind-4 residual debt on its own
# merits (its writes land 2+ layers down in ~15 handler-owned op modules), but
# that debt is no longer visible to THIS gate and is not tracked by a row here.
#
# `check-auto-reconcile.py` (triaged 2026-08-06): already reaches the engine
# through `coordinator_core.invoke.dispatch.dispatch_message` — the very
# chokepoint this seam exists to reach. Routing it through run_op_main would be
# a cosmetic no-op over an already-compliant path.
#
# `orphan-branch-sweep.py` and `day-coverage-sweep.py` (triaged 2026-08-06):
# both structurally read-only — they enumerate and report to stdout, and
# neither op has a write site to declare. Routing them through the seam buys an
# import hop and a permanently-empty declaration list. NOTE: orphan-branch-
# sweep.py briefly LOOKED converted because it carries a comment mentioning
# run_op_main, and the gate's seam check was a substring scan; that hole is
# fixed (the check is AST-based on both sides now, requiring a real call), and
# this pair is the reason the fixture for it exists.
coordinator/bin/audit-enabled-plugins.py
coordinator/bin/baton-drift-sweep.py
coordinator/bin/check-auto-memory-drained.py
coordinator/bin/check-auto-reconcile.py
coordinator/bin/check-description-length.py
coordinator/bin/check-harvest-debt.py
coordinator/bin/check-shipped-on-main.py
coordinator/bin/check-surface-inline-budget.py
coordinator/bin/coordinator-fold-execution-record.py
coordinator/bin/coordinator-prepare-commit-msg
coordinator/bin/coordinator-prepare-commit-msg.py
coordinator/bin/coordinator-safe-commit.py
# `cross-repo-memo.py` (added 2026-08-14, C5b): its ONE in-process op reach
# that this gate credits is `_commit_archived_outbox_draft`'s
# `from coordinator_core.ops.ceremony import git_native` (line ~3234) —
# `git_native.commit_scoped`, called to best-effort commit the just-archived
# outbox draft into the SENDING repo immediately after the send. This is not
# an op with a `main(argv)` to route through `run_op_main`: `git_native` is a
# low-level commit primitive, and the call site already performs its own
# scoped `git commit` synchronously rather than staging a path for the
# session's later batched `scoped_git_commit` — there is nothing for
# `declare_write`/`recording_declared_writes` to claim, since the write is
# already committed by the time this function returns. (The file's other
# `coordinator_core.ops` reference, `ops.fleet._outbox_frontmatter_rules`, is
# already exempt under `_is_private_ops_module` — every bound name there is
# private — and is not why this entry exists.)
coordinator/bin/cross-repo-memo.py
coordinator/bin/day-coverage-sweep.py
coordinator/bin/dirty-tree-gate.py
coordinator/bin/emit-goal-from-artifact.py
coordinator/bin/generate-repomap.py
coordinator/bin/handoff-deliverable-carry.py
coordinator/bin/lessons-outbox-drain.py
coordinator/bin/list-orphaned-plans.py
coordinator/bin/orphan-branch-sweep.py
# `percolate-round.py` (added 2026-08-25): `scoped-git-commit` (the
# `ceremony.scoped_git_commit` CLI) was killed 2026-08-23 (PM ruling,
# DR-344) -- deleted, not suspended, and not replaced by any registered op
# `run_op_main` can route to. This CLI's commit step was re-pointed at
# `commit_pipeline.run_commit_pipeline` in-process instead, mirroring
# `publish.py::_commit_published_dests` immediately below -- same
# mechanism (the killed CLI was a trampoline over this exact function),
# now the sanctioned in-process path since its own wrapper op is gone. See
# the call site's own comment in `percolate-round.py` for the full
# rationale.
coordinator/bin/percolate-round.py
coordinator/bin/probe-cwd-example-retrieval-repo-relevance.py
# `query-commit-closures.py` (added 2026-08-25): same "porter trampoline"
# shape as `query-goals.py`/`query-completion-rollups.py`/`query-routine-
# signals.py` -- it porters `sections/commit_closures.py:collect(ctx)`, an
# emit *section* with NO registered op behind it, so there is no op name
# for `run_op_main` to route to. See its own module docstring for the full
# reasoning (explicitly cites `query-goals.py`'s as the fuller version).
coordinator/bin/query-commit-closures.py
# `query-goals.py` and `query-completion-rollups.py` (added earlier, both
# labeled "porter trampolines"): same shape as `query-routine-signals.py`
# immediately below -- each porters an emit *section*
# (`sections/goals.py:collect`, `rollups.collect`) that has NO registered op
# behind it, so there is no op name for `run_op_main` to route to. Both
# deliberately reuse `op_trampoline.resolve_repo_root_or_exit()` /
# `resolve_claude_klabauter_root_or_exit()` only, per their own module docstrings,
# which cite this exact reasoning and this exact sibling file.
coordinator/bin/query-completion-rollups.py
coordinator/bin/query-goals.py
# `query-record-history.py` (added 2026-08-25): its ONE in-process op reach
# this gate credits is `_supported_types_hint`'s best-effort `from
# coordinator_core.ops.record_history import supported_record_types` --
# the same precedent `query-records.py::_list_schemas` already establishes
# for reaching engine-side metadata from a CLI, wrapped in a bare `except
# Exception` and used only to enrich an error message. The actual op
# dispatch goes through `cc_invoke.route()` -- already on the wire path,
# same reasoning as `check-auto-reconcile.py` above -- so there is nothing
# for `declare_write`/`recording_declared_writes` to claim here either.
coordinator/bin/query-record-history.py
# `query-routine-signals.py` (added 2026-08-15): its ONE in-process op reach
# this gate credits is `main()`'s `from coordinator_core.ops.emit.sections
# import routine_signals` — `routine_signals.collect(ctx)` is a plain
# in-process function (an emit *section*), not a registered op with a
# `main(argv)` for `run_op_main` to route to; the file's own module docstring
# explains at length why it deliberately does NOT use
# `coordinator/bin/lib/op_trampoline.py`'s `run()` for this call, and that
# minting a registered op for `routine_signals.collect` is explicitly the
# engine change this porter's plan (`docs/plans/
# 2026-08-11-three-trampolines-and-the-bare-repo-producer.md` § C4) rules out.
# The CLI is read-only — it prints the six computed RoutineSignal records to
# stdout and writes nothing to disk — so there is no write for
# `declare_write`/`recording_declared_writes` to claim.
coordinator/bin/query-routine-signals.py
coordinator/bin/probe-onboarding-currency.py
coordinator/bin/prune-closed-improvements.py
coordinator/bin/publish.py
coordinator/bin/review-brightline-gate.py
coordinator/bin/safe-commit-offer.py
coordinator/bin/spinoff-deliverable-and-commit.py
coordinator/bin/sweep-boot.py
coordinator/bin/sync-main.py
coordinator/bin/update-docs-probes.py
coordinator/bin/verify-orientation-cache-sync.py
coordinator/bin/verify-parallel-review-lens-orthogonality.py
coordinator/bin/workday-complete-backfill-scan.py
coordinator/bin/workday-start-advisory-counters.py
coordinator/bin/workweek-complete-advisories.py
coordinator/bin/workweek-complete-doc-staleness.py
coordinator/bin/wsc-coverage-gate-runner.py
