"""Formerly: C5's re-emit-and-diff proof that the 6 named contradictory-status
forks closed under C4's shared canonicalization read-model.

`state/deliverable-equivalence.yaml` + `canonicalize()` are condemned and
collapsed to identity (plan 2026-08-20-the-close-ceremony-stops-paying-for-
the-join, F-1). The join this fixture proved closing no longer exists --
`deliverable_status._compute_map` groups strictly on raw `deliverable_id`
now, so the 6 named pairs stay independent groups by construction and there
is nothing left for this fixture to prove.

Spec backlink: pln-deliverable-id-fork-remediatio-894e26 § C5 (AC8) --
superseded.
"""

from __future__ import annotations
