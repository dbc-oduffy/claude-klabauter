---
description: Guides the EM through structured plan authoring, substrate verification, and PM-lens review.
version: 1.0.0
---
Negative fixture — missing required `name` field. Verifies that the skill schema validator rejects records missing a required field. The `name` key is intentionally absent from the frontmatter above.
