{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coordinator.local/schemas/prior-art-check.schema.json",
  "x-schema-name": "prior-art-check",
  "x-schema-version": "1.1.0",
  "applies_to": "state/plan-sidecars/*.prior-art-check.md",
  "kinds": ["prior-art-check"],
  "description": "Prior-art-check sidecar — records conflicts/compatible/silent counts from a pre-review prior-art sweep. Lives at the plan-derivable state/plan-sidecars/<plan-stem>.prior-art-check.md home (moved out of docs/plans/ by docs/plans/2026-07-24-g2-plan-pipeline-sidecar-contract.md C11/D0). applies_to covers both canonical *.prior-art-check.md and legacy *.md.prior-art-check.md forms. status is NOT enumerated — legacy values like 'reviewed'/'complete' vary on disk.",
  "type": "object",
  "required": ["plan"],
  "properties": {
    "plan": {
      "type": "string",
      "description": "Repo-relative path to the plan this prior-art check covers."
    },
    "author": {
      "type": "string",
      "description": "Agent that ran the prior-art check."
    },
    "created": {
      "type": "string",
      "format": "date",
      "description": "Date this sidecar was authored (YYYY-MM-DD)."
    },
    "status": {
      "type": "string",
      "description": "Lifecycle state. Not enumerated — legacy values like 'reviewed'/'complete' vary."
    },
    "conflicts": {
      "type": "number",
      "description": "Count of prior-art findings that conflict with this plan."
    },
    "compatible": {
      "type": "number",
      "description": "Count of prior-art findings that are compatible-but-relevant."
    },
    "silent": {
      "type": "number",
      "description": "Count of prior-art findings that are silent (no impact)."
    },
    "title": {
      "type": "string",
      "description": "Human-readable label for this prior-art check sidecar."
    }
  }
}
