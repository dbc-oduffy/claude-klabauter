{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coordinator.local/schemas/lessons-outbox.schema.json",
  "x-schema-name": "lessons-outbox",
  "x-schema-version": "1.0.0",
  "applies_to": "state/lessons-outbox/*.yaml",
  "match_mode": "whole-document-yaml",
  "description": "Lessons-outbox artifact — a universal-scope lesson routed by coordinator-lesson-promote toward a central-wiki fold-in. Stored per-lesson in state/lessons-outbox/<topic>.yaml (whole-document YAML — no prose markdown body).",
  "type": "object",
  "required": ["id", "created", "from_repo", "change_kind", "target_wiki", "title", "body"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable identifier for this lessons-outbox entry."
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp this entry was authored. Producer coordinator-lesson-promote stamps _now_iso() date-time; field matches producer reality — do not narrow to date-only."
    },
    "from_repo": {
      "type": "string",
      "description": "Repo the lesson originated in."
    },
    "change_kind": {
      "type": "string",
      "enum": ["doctrine-edit", "agent-prompt-edit", "hook-edit", "script-edit", "snippet-sync-update", "wiki-new", "wiki-append", "skill-edit"],
      "description": "The kind of change this lesson recommends downstream."
    },
    "target_wiki": {
      "type": "string",
      "description": "The wiki (or other doc surface) this lesson should fold into."
    },
    "title": {
      "type": "string",
      "description": "Short human-legible title for this lesson."
    },
    "body": {
      "type": "string",
      "description": "Full prose content of the lesson."
    },
    "scope_tags": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Optional classification tags for this lesson."
    },
    "evidence": {
      "type": "string",
      "description": "Optional supporting evidence (path, quote, or reference) backing this lesson."
    }
  }
}
